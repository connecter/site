language: node_js

node_js:
  - "4.1"
  
before_install:
  - echo "node version $(node -v) running"
  - echo "npm version $(npm -v) running"
  
install:
  - bundle install
  
before_script:
  - npm install -g gulp
  
script:
  - gulp build